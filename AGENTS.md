# AGENTS.md - AI 어시스턴트 공용 작업 지침

이 저장소에서 일하는 **모든 AI 코딩 어시스턴트**(Claude Code, Codex, Gemini, Antigravity 등)가 따르는 단일
원본 문서다. `CLAUDE.md`는 이 파일을 불러오는 한 줄(`@AGENTS.md`)만 갖고 있으니, 지침은 **이 파일만** 고친다.
자세한 사고 경위는 `history.log`(버전별)와 코드 주석(`🚨 [날짜 실전 확인]` 형식)에 있다 - 여기엔 규칙과
한 줄짜리 대표 사례만 둔다.

---

## 1. 프로젝트 한눈에 보기

### 무엇인가
- 모바일 게임 **위저드리 다프네(한국어판)** 자동 파밍 매크로. Python + ADB(`ppadb`) + OpenCV 템플릿 매칭
  (`cv2.matchTemplate`, `TM_CCOEFF_NORMED`). 사용자가 개인용으로 쓰며 가끔 GitHub 릴리즈로 배포.
- 환경: **MuMu Player 안드로이드 15 인스턴스 전용**(v1.20.0~, 기본 ADB 포트 `16448`), 해상도 **1440x2560
  세로 고정** - 모든 좌표/도장/ROI가 이 해상도 기준 픽셀값.
- `wvd-master/`는 같은 게임의 중국산 매크로(1600x900). **분석 전용** - 배포/릴리즈 포함 금지, 도장은 해상도가
  달라 그대로 못 씀. 뮤뮤/게임 환경 문제로 막히면 `wvd-master/src/`를 먼저 grep해볼 것(해법이 있었던 적 있음).

### 실행 흐름
1. 루트의 프리셋별 `.bat`(예: `대설지대6층 상자파밍.bat`)이 영문 프리셋ID를 `daphne_preset_id.txt`에 쓰고
   `python src/main.py`를 실행. `.bat`은 **외부 슈퍼바이저 루프**를 겸한다 - python이 완전히 죽으면
   `macro.pid`의 프로세스가 5초 연속 없는지 확인한 뒤 재기동(`os.execv` 자체재시작은 윈도우에서 새 PID로
   떠서 `python` 줄이 즉시 리턴되므로 그걸 사망으로 오판하지 않게 설계됨).
2. `main.py`가 `PRESET_ID_MAP`으로 한글 프리셋명을 찾고 `src/presets.json`에서 마을/던전/층/주회방식을
   읽는다(스키마 설명은 그 파일의 `_가이드` 키). 공통 설정(주회 한도, 힐 주기, 슬롯 번호)은 `main.py`
   최상단 "마스터 글로벌 제어" 변수.
3. `main.py`의 `start_grand_orchestrator()`(=사령탑)가 **던전 밖**(타이틀/마을/여관/세계지도/던전선택/
   마을외곽)을 화면 분류로 처리하다가, 던전에 들어가면 `dungeon_bot.start_main_macro()`로 넘긴다.
4. `start_main_macro()`는 **던전 안** 상태머신(FIELD_WAIT / AUTO_MOVING / IN_COMBAT / TRIGGER_EXIT /
   BRANCH_CHECK / PLAY_MINIGAME / CLEAR_CHECK). 매 틱: 상태와 무관한 공용 전처리 블록(하켄 메뉴, 캠핑,
   상인/중립몹/뼈상인 대화, 대화 화살표 등) → 상태별 분기. 정체 판정은 `last_state_changed_time`(상태가
   바뀌면 리셋) 기준 - 30초 KEYCODE_BACK, 60초 0.45 완화 가드, 300초 `RuntimeError`로 강제 재시작.
5. 안전망 3겹: 프로세스 내 Watchdog 스레드(4분 무반응 → 자체재시작), 300초 정체 하드리밋, `.bat` 슈퍼바이저.

### 파일 역할 (`src/`)
| 파일 | 역할 |
|---|---|
| `main.py` (~220KB) | 사령탑. 기동/ADB 연결/앱 복구/재시작(`restart_process`, `os.execv`)/로그/Watchdog/화면 분류 |
| `dungeon_bot.py` (~330KB) | 던전 내부 상태머신 + 상자/전투/귀환/캠핑/대화. 거대 단일 함수 `start_main_macro()` |
| `chest_opener.py` | 상자 해제(따개 캐릭 선택, 함정 미니게임 난타) |
| `party_manager.py` | 파티 힐링(힐러방) 시퀀스 |
| `inn_manager.py` | 여관 숙박 시퀀스 |
| `combat_manager.py` | 구형 스킬 시스템(폐기 예정 - ROADMAP 2번) |
| `screen_capture.py` | 원시(raw) ADB 캡처(PNG 대비 2.5배 빠름, 실패 시 PNG 폴백) |
| `mumu_display_check.py` | 연결 직후 디스플레이 개수 점검(2개 이상이면 정지 - §3 참고) |
| `presets.json` | 프리셋 정의 |

⚠️ 각 파일이 `load_template` 등 로더를 **각자 복사해서** 갖고 있다(import 공유 아님 - §4 참고). 단
`main.py`가 `dungeon_bot.<함수>`를 호출하거나 `dungeon_bot.<전역>`을 세팅하는 교차 호출은 의도된 패턴.

### 폴더
- `templates/` - 도장(PNG). 폴더별 `_crop_metadata.json`에 원본 스샷 경로/ROI(있는 폴더만).
  `templates/village_common/README.md`에 마을 공용 도장 실측 표.
- `logs/` - 실행 로그(`YYYY-MM-DD-HHMM-NNN_start|rebootN.txt`) + 정체/재시작 증거 스샷(4일 후 자동 삭제).
  **버그 조사는 여기서 시작**(§7).
- `dev/` (git 미추적) - **`ROADMAP.md`(추후 구현 목록 - 새 세션은 이것부터 읽을 것)**, `ROI_check/`(실측용
  원본 스샷), `template_cropper/`(도장 크롭 GUI), `tools/make_release_zip.py`, `AutoSkillCast/`(폐기 예정
  프로토타입), `fog_check/`.
- `remote_control/` - 폰(테일스케일)에서 시작/정지/상태 확인하는 로컬 HTTP 서버(표준 라이브러리만).
  `/stop`은 python.exe와 루트 cmd.exe(`macro_cmd.pid`)를 같이 종료.
- `releases/` - 릴리즈 zip 백업(**수정/삭제 절대 금지**).
- `history.log` - 버전별 상세 이력. "왜 이렇게 짰지?"의 답은 대부분 여기와 코드 주석에 있다.

### 자주 쓰는 명령
```bash
python -m py_compile src/main.py src/dungeon_bot.py      # 수정 후 항상
# 라이브 화면 확인(Git Bash). ⚠️ §2 - 먼저 사용자에게 물어볼 것
adb -s 127.0.0.1:16448 shell "screencap -p /sdcard/chk.png"
MSYS_NO_PATHCONV=1 adb -s 127.0.0.1:16448 pull /sdcard/chk.png "dev/ROI_check/chk.png"
PYTHONIOENCODING=utf-8 python dev/tools/make_release_zip.py   # 릴리즈 zip(지시받았을 때만)
```

### 용어
"도장"=템플릿 이미지, "앵커"=화면 판별용 도장, "하켄"=던전 내 귀환 포탈, "백아"=백아의 동굴,
"유령성"=북쪽의 유령선(FFXI 콜라보), "대설지대"=이스벨크 마을외곽 경유 던전, "피안개/딸피 장막"=주인공
빈사 시 화면에 씌워지는 붉은 연출, "뮤뮤"=MuMu Player.

---

## 2. 사용자와 일하는 방식

- **호칭**: 사용자를 "삶은계란"이라고 부른다(GitHub 닉네임 "life is egg").
- **정리 먼저, 코딩은 지시 후**: 버그를 진단하거나 개선점을 찾으면 원인/증상/수정 방향을 정리해 보고하고
  멈춘다. "코딩해"/"진행해"/"~하자"처럼 명시적으로 지시받기 전엔 코드를 고치지 않는다(위급해 보여도 동일).
  당장 안 하기로 한 건 `dev/ROADMAP.md`에 남긴다.
- **여러 요청이 한 메시지에 오면** 항목을 먼저 목록으로 정리하고 하나도 빠짐없이 답/조치한다. 일부러 미루는
  항목도 "미룬다"고 명시한다(누락 경향을 사용자가 여러 번 지적함).
- **수정 보고는 두 가지를 반드시**: (a) 정확히 어떻게 바뀌었는지(이전 동작 vs 이후 동작), (b) 정확히 어떤
  효과가 기대되는지. "고쳤습니다"만으로 끝내지 않는다.
- **추측 금지**: 임계값/색상/좌표/도장은 실제 스크린샷 또는 실제 로그로 검증한 뒤 반영한다. 실측을 못 했으면
  "실측하지 못했다"고 명시한다.
- **라이브 뮤뮤에 ADB로 붙기 전엔 먼저 묻는다**(screencap/tap/shell 등). 사용자가 직접 조작 중인 경우가 많아
  말없이 붙으면 진단이 오염된다. 저장된 로그/스샷 읽기, 또는 사용자가 방금 "화면 확인해봐"라고 한 경우는
  예외.
- **커밋은 로컬에서 자유롭게, `git push`/GitHub 릴리즈는 명시적 지시가 있을 때만.** 실전 검증이 쌓일 때까지
  묶어서 내보내는 걸 선호한다.
- **`releases/`는 백업 전용** - 어떤 파일도 수정/삭제/덮어쓰기 금지.

---

## 3. 환경 함정 (코드 버그로 오진하기 쉬운 것)

- **MuMu '앱 상주'(디바이스 설정 → 기타 → "애플리케이션이 실행 중입니다")는 반드시 꺼져 있어야 한다.** 켜지면
  디스플레이가 여러 개가 되어 (1) 안드15 `screencap`이 PNG 앞에 경고문을 붙여 캡처가 깨지고 (2) 탭이
  엉뚱한 디스플레이로 샌다. 확인: `adb shell dumpsys SurfaceFlinger --display-id`. `mumu_display_check.py`가
  2개 이상이면 매크로를 멈춘다 - **캡처만 우회하는 식으로 고치지 말 것**(화면은 읽는데 클릭은 홈 화면으로
  나가는 더 위험한 상태가 됨).
- **"화면이 멈췄다"류 정체가 코드로 설명되지 않으면 GPU 드라이버 크래시(TDR)부터 의심.** 윈도우 이벤트
  뷰어에서 `nvlddmkm` / Event ID 4101을 **며칠 범위로** 조회. 현재 안정 설정: 안드15 인스턴스 + 30fps +
  **OpenGL** 렌더러 + GPU "메모리 사용 감소". (Vulkan/DirectX는 실패했음, 안드12는 렌더러 에러로 지원 중단)
- **도장 파일명은 ASCII(영어)만.** 이 환경의 `cv2.imread`는 한글/중국어 경로면 조용히 `None`을 돌려준다.
- 뮤뮤는 설정을 메모리에 들고 있다가 종료 시에만 `customer_config.json`에 쓴다 - 디스크 JSON과 실제 UI가 다를 수 있음.

---

## 4. 도장(템플릿) 인식 규칙

### 4-1. 새 도장을 추가할 때
- 사용자에게 "배경이 반투명이라 상황에 따라 달라지나요?"를 먼저 묻고, 반투명이면 이진화(160), 불투명이면
  그레이스케일을 근거와 함께 추천. (예외: 마을 공용 도장은 실측상 그레이스케일이 나았음 -
  `village_common/README.md`)
- ROI를 쓰면 도장 크기 대비 ROI 여백이 충분한지 수치로 확인해 보고.
- 새 앵커는 **다른 화면 스크린샷 전부에 돌려 오탐 점수를 잰다.** 오탐이 있으면 배제 조건을 같이 넣는다.
- wvd에서 빌려온 도장은 영어 이름 + `_wvd` 접미사(`Gerard_sp_wvd.png`), 릴리즈에서 제외.

### 4-2. 신뢰도 임계값은 낮추지 말고, 이진화 문턱만 여러 개 시도(멀티패스)
- 배경이 바뀌는 화면(피안개 같은 일시 연출, 스토리 진행에 따른 배경 교체)에서는 고정 이진화 문턱 하나가
  안 맞아 점수가 무너진다. 사례: 유령성 진엔딩 후 던전선택 배경 교체로 0.72(기준 0.88) → 밤새 7시간 진입 실패.
- **선택지형 화면**(던전선택, 메뉴, 대화 선택지)에서 버튼을 찾아 누르는 코드는 멀티패스 이진화가 기본 -
  `dungeon_bot.check_template_present_multipass()`(`FOG_BIN_PASSES=(160,100,85)`) 또는
  `main.find_and_click_template_multipass()`(`bin_passes=(160,150)`)를 재사용.
- **매 틱 호출되는 상시 감시 앵커**(배속/자동전투/HP)는 비용 때문에 멀티패스로 바꾸지 않는다.
- 새 문턱 조합은 문제가 재현된 **실제 스크린샷으로 측정**해서 정한다(열화 원인마다 필요한 방향이 다름 -
  다른 상황의 문턱 세트를 재사용하지 말 것).
- 참고: 기존 "정체 탈출 가드"(60초 정체 시 임계값을 0.45로 낮춤)는 이 원칙에 어긋나는 레거시이고 실제로
  오탐 사고를 두 번 냈다 - 제거 검토 중(ROADMAP 17번). 이런 방식을 새로 만들지 말 것.

### 4-3. 인식 방식을 고치면 같은 도장·같은 앵커를 쓰는 곳을 전부 점검
1. 도장 **파일명**으로 grep(`grep -rn "combat_in.png" src/*.py`)해서 다른 파일도 같은 로더/매칭 방식인지 확인.
2. 도장을 담은 **변수명**으로도 grep(`t_combat_in`)해서 모든 호출부의 임계값을 확인 - **같은 파일 안이라도
   예외 없다.** 사례: 동결 상태이상으로 전투 앵커가 0.75로 떨어졌는데, 0.80 하드코딩이 main.py 1곳 +
   dungeon_bot.py 안에만 7곳 흩어져 있었음. 값이 다르면 의도된 차이인지 방치된 불일치인지 구분하고, 이유가
   없으면 실전 검증된 값으로 통일.
3. 파일마다 로더가 다르면(이진화 vs 그레이스케일, ROI 유무) 왜 다른지 먼저 확인. 방치된 잔재면 방금 검증된
   방식으로 맞춘다. 사례: main.py만 옛 이진화로 전투를 판정해 스킬 메뉴 화면에서 5분마다 헛재부팅.
4. 도장을 파라미터로 넘기는 함수는 **그 파라미터가 실제로 쓰이는지** 확인(죽은 파라미터 사례:
   `run_party_healing_sequence(t_auto_btn, t_close_btn)`).
5. 도장 **경로가 실제로 존재하는지**도 확인 - 로더는 파일이 없으면 조용히 `None`을 돌려주고 매칭 함수는
   `None`이면 `False`라 가드가 소리 없이 죽는다(사례: `party_manager.py`가 폴더 이사 전 옛 경로
   `templates/yeolda_clean.png`를 로드 - ROADMAP 19번).
6. 수정 후 실제 스크린샷(또는 로그 스코어)으로 새 방식이 통과하는지 재확인.

---

## 5. 상태머신·카운터 규칙 (`dungeon_bot.start_main_macro()`)

이 함수는 수십 개의 실전 핫픽스로 커진 거대 단일 함수다. 전면 재작성은 리스크가 커서 하지 않기로 했고,
아래 규칙으로 재발을 막는다. 공통 교훈: **새 코드가 기존 패턴의 "주된 동작"만 복제하고 부수효과(카운터/
플래그 리셋)를 빠뜨려도 에러 없이 돌다가, 장시간 무인 방치 중에만 무한루프로 터진다.**

1. **화면 진행 감지 분기에는 `transition_delay_count = 0`도 같이.** 필드 안착/전투 조우/사망 감지/하켄 처리
   등을 새로 감지하는 분기를 넣으면, 공용 전처리 블록의 비슷한 감지 로직 옆 리셋을 똑같이 넣는다. 같은
   감지 로직이 여러 곳에 있으면 한쪽만 리셋하고 있지 않은지 diff. (`return`/`raise`로 빠지는 분기는 불필요)
   사례: 유령성 3층→4층 두 번의 로딩이 하나의 10회 예산으로 합쳐져 카운트됨.
2. **"몇 번 시도 후 다음 수단으로" 에스컬레이션 카운터를 공용 상태전환 리셋에 물리지 말 것.** 루프의
   범용 `else:`(state가 바뀌면 `last_state_changed_time` 등 리셋)는 복구 동작이 만든 **허상의 상태 왕복**에도
   걸린다. 이런 카운터는 전용 변수로 두고 "진짜 해결" 이벤트나 자체 시간창으로만 리셋한다. 사례:
   `minigame_reentry_count`(FIELD_WAIT↔PLAY_MINIGAME 왕복 554회), `low_threshold_reset_count`(8시간 동안
   400회 전부 "1/1" - 탈출 단계가 한 번도 실행 안 됨).
3. **추측으로 하는 동작(블라인드 탭, KEYCODE_BACK)은 정체 타이머를 리셋하지 않는다** - 그래야 추측이
   계속 실패해도 300초 하드리밋이 안전망으로 작동한다.
4. **새 던전/새 귀환 경로를 짤 땐 기존 던전의 같은 지점을 나란히 놓고 비교**한다
   (`grep -n "dungeon_run_count = 0" src/main.py`처럼 리셋 지점 목록 확인). 따로 체크할 것: 되돌려야 하는
   카운터(`dungeon_run_count`, `transition_delay_count` 등)와 다음 단계 트리거 플래그(`is_fully_healed`,
   `need_pickaxe_refill` 등). 의도적으로 다르게 짰으면 이유를 주석으로. 사례: 대설지대 여관 경유에서
   `dungeon_run_count = 0`만 빠져 "마을외곽↔경로목록" 2시간 무한 왕복.
5. 가능하면 그 조건(정체 임계값, 주회 한도)에 실제로 도달하는 시나리오까지 재현해 검증 - 짧은 테스트
   주행으로는 이런 결함이 안 드러난다.

### 새 던전 추가 요청을 받으면
사용자에게 받을 정보: 던전명 / 진입 방식(세계지도 직행형 vs 마을 경유형) / 출발·복귀 마을 / 필요한 도장
(세계지도 아이콘, 던전선택 앵커, 마을외곽 버튼·경로 행, 층 버튼 - 반투명 여부 포함) / 층 목록과 층별 주회방식
(상자파밍·광석파밍) / 귀환 방식(`presets.json` `_가이드`의 `return_method`) / 특수 파밍 도장.
코드 등록 지점: `presets.json` 프리셋 + `main.py` `PRESET_ID_MAP` + 루트 `.bat`(기존 것을 복사해 영문ID·
title만 교체 - 슈퍼바이저 루프 유지) + 세계지도 아이콘 분기(세 번째 마을/직행형 던전이면 ROADMAP 10번
딕셔너리 리팩터링부터).

---

## 6. 새 코드를 짤 때의 교훈 (대설지대 구현 재검토에서 나온 버그 8건의 공통 패턴)
1. **비슷한 두 화면을 도장으로 구분하려 들지 말 것** - 같은 단어/모양 도장은 서로 교차 매칭된다(0.895).
   같은 동작을 원하면 "동작 반복 + 다른 화면의 명확한 앵커로 탈출"하는 수렴 루프로.
2. **"이 앵커는 이 화면에만 있겠지" 금지** - "아무것도 안 한다"가 하켄/상자/캠핑 화면에 전부 있었다.
3. **UI 상대 위치가 항상 같다고 가정 금지** - 화면에 하나뿐인 UI는 전체검색(가장자리에서 121px 밀림).
4. **항상 보이는 버튼을 상태 신호로 쓰지 말 것** - 재개 버튼은 필드에 항상 있다. 활성/비활성처럼 밝기만
   다른 도장은 `TM_CCOEFF_NORMED`가 밝기차를 정규화해 구분 못 한다 - 그 자리의 평균 밝기로 판정.
5. numpy 배열을 `==`로 센티넬 비교하지 말 것(`ValueError`가 상위 `except`에 삼켜져 기능이 통째로 죽음).
6. 알려진 선택지 화면 처리(`_handle_dungeon_interrupt`)는 **대화 화살표 탭보다 먼저** - 화살표만 누르면
   선택이 안 되는 화면에서 무한 반복한다(사례: 울타리 구멍 화면 8시간 16,661회).

---

## 7. 버그 조사할 때 쓸 원본 자료 - 채팅 첨부 이미지에 의존하지 말 것
1. 사용자가 로그 파일명을 주면 `logs/`에서 직접 읽는다. 정체 시점엔 `logs/<시각>_stuck_<상태>.png` 증거
   스샷이 자동 저장돼 있다.
2. 붙여넣은 로그에 "📸 [스크린샷 동기화] ... 카피되었습니다: `<파일명>`" 줄이 있으면 그 파일이 `logs/`에 있다 -
   사용자가 그 순간 직접 캡처한 경우가 많다.
3. 기존 도장의 원본 화면이 필요하면 같은 폴더 `_crop_metadata.json`의 `source_image`를 보고
   `dev/ROI_check/`에서 찾는다(크롭 조각보다 오탐/위치/밝기 검증에 훨씬 유리).
4. 위에서 못 찾았을 때만 채팅 이미지 설명에 의존하고, "실측하지 못했다"고 명시.

---

## 8. 추후 구현 예정 사항 관리
- "나중에 하자"로 합의한 건 전부 **`dev/ROADMAP.md` 하나**에 적는다. 루트나 `dev/`에 별도 메모 파일을 만들지
  말 것(예전에 흩어진 메모 11개를 2026-09-24에 통합 후 삭제함).
- 구현이 끝나면 **그 항목을 지운다**(이력은 `history.log`와 커밋에 있음). 일부만 됐으면 남은 부분만 남긴다.
- 폐기한 아이디어는 재검토 방지용으로 "❌ 검토 후 폐기" 섹션에 이유와 함께 남긴다.

---

## 9. 코드 수정·릴리즈 절차
- 코드를 고치면: `history.log` 최상단에 상세 이력(무엇을/왜/어떻게/검증/적용파일) + 해당 `src/` 파일 상단
  버전 히스토리 주석에 한두 줄. 릴리즈 전 변경은 `[미릴리즈 - 다음 버전에 포함]` 항목에 모아둔다.
- **기존 검증된 시퀀스를 최우선 재사용**하고, 임시 분기나 억지 예외(`or 특정던전`)를 새로 만들지 않는다.
- **던전 내 상자 최우선 철칙**: 무슨 작업 중이든 "열다"(`t_yeolda`)가 보이면 `chest_opener.open_and_disarm_chest`.
- 릴리즈(지시받았을 때만): `CURRENT_VERSION`(main.py) + `main.py`/`dungeon_bot.py`/`remote_control/server.py`
  버전 히스토리 주석 + `README.md` 배너 + `history.log` 갱신 → `PYTHONIOENCODING=utf-8 python
  dev/tools/make_release_zip.py`(git 추적 파일 전부, AI 지침 파일과 wvd 자산만 제외 - 손으로 압축하지 말 것)
  → `git tag -a vX.Y.Z` → push → `gh release create`. README 이미지는 `readme_img/`에 두고 상대경로로.
