# 코딩 컨벤션 (AI 어시스턴트용)

이 저장소에서 작업하는 모든 AI 코딩 어시스턴트(Claude 외 다른 도구 포함)가 따라야 할 규칙입니다.
동일 내용이 `AGENTS.md`에도 있습니다(수정 후엔 항상 `cp CLAUDE.md AGENTS.md`로 동기화).

## 새 세션 시작 가이드 - 프로젝트 한눈에 보기 (2026-09-24 정리)

### 무엇인가
- 모바일 게임 **위저드리 다프네(한국어판)** 자동 파밍 매크로. Python + ADB(`ppadb`) + OpenCV 템플릿
  매칭(`cv2.matchTemplate`, `TM_CCOEFF_NORMED`). 사용자(GitHub "life is egg", 호칭 "삶은계란")가 혼자
  쓰며 가끔 GitHub 릴리즈로 배포.
- 환경: **MuMu Player 안드로이드 15 인스턴스 전용**(v1.20.0부터, 기본 ADB 포트 `16448`), 해상도
  **1440x2560 세로 고정** - 모든 좌표/도장/ROI가 이 해상도 기준 픽셀값이다.
- `wvd-master/`는 참고용 중국산 매크로(1600x900, 코드 역분석 전용 - 배포/릴리즈 절대 포함 금지, 도장은
  해상도가 달라 그대로 못 씀).

### 실행 흐름
1. 루트의 프리셋별 `.bat`(예: `대설지대6층 상자파밍.bat`)이 영문 프리셋ID를 `daphne_preset_id.txt`에 쓰고
   `python src/main.py` 실행. `.bat`은 **외부 슈퍼바이저 루프**를 겸함 - python이 완전히 죽으면
   `macro.pid`로 진짜 사망을 5초간 확인한 뒤 재기동(`os.execv` 자체재시작은 윈도우에서 새 PID로
   떠서 `python` 줄이 즉시 리턴되므로, 그걸 사망으로 오판하지 않도록 설계됨).
2. `main.py`가 `PRESET_ID_MAP`으로 한글 프리셋명을 찾고 `src/presets.json`에서 마을/던전/층/주회방식을
   읽는다(스키마 설명은 그 파일의 `_가이드` 키). 공통 설정(주회 한도, 힐 주기, 슬롯 번호 등)은
   `main.py` 최상단 "마스터 글로벌 제어" 변수.
3. `main.py`의 `start_grand_orchestrator()`(=사령탑)가 **던전 밖**(타이틀/마을/여관/세계지도/던전선택/
   마을외곽)을 화면 분류로 처리하다가 던전에 들어가면 `dungeon_bot.start_main_macro()`로 넘긴다.
4. `dungeon_bot.start_main_macro()`는 **던전 안** 상태머신(FIELD_WAIT / AUTO_MOVING / IN_COMBAT /
   TRIGGER_EXIT / BRANCH_CHECK / PLAY_MINIGAME / CLEAR_CHECK). 매 틱 상태와 무관하게 도는 공용
   전처리 블록(하켄 메뉴, 캠핑, 상인/중립몹/뼈상인 대화, 대화 화살표 등) → 상태별 분기 순서.
   정체 판정은 `last_state_changed_time`(상태가 바뀌면 리셋) 기준 - 30초 KEYCODE_BACK, 60초 0.45 완화
   가드, 300초 `RuntimeError`로 강제 재시작.

### 파일 역할 (`src/`)
| 파일 | 역할 |
|---|---|
| `main.py` (~220KB) | 사령탑. 기동/ADB 연결/앱 복구/재시작(`restart_process`, `os.execv`)/로그/Watchdog/화면 분류 |
| `dungeon_bot.py` (~330KB) | 던전 내부 상태머신 + 상자/전투/귀환/캠핑/대화 처리. 거대 단일 함수 `start_main_macro()` |
| `chest_opener.py` | 상자 해제(따개 캐릭 선택, 함정 미니게임 난타) |
| `party_manager.py` | 파티 힐링(힐러방) 시퀀스 |
| `inn_manager.py` | 여관 숙박 시퀀스 |
| `combat_manager.py` | 구형 스킬 시스템(폐기 예정 - ROADMAP 2번) |
| `screen_capture.py` | 원시(raw) ADB 캡처(PNG 대비 2.5배 빠름, 실패 시 PNG 폴백) |
| `mumu_display_check.py` | MuMu 디스플레이 설정 점검 |
| `presets.json` | 프리셋 정의 |

⚠️ 각 파일이 `load_template` 등 로더를 **각자 복사해서** 갖고 있다(import 공유 아님) - 아래
"도장 인식 방식을 고칠 때" 규칙 참고. 단 `main.py`가 `dungeon_bot.<함수>`를 직접 호출하거나
`dungeon_bot.<전역>`을 세팅하는 교차 호출은 의도된 패턴이다.

### 폴더
- `templates/` - 도장(PNG). 폴더별 `_crop_metadata.json`에 원본 스샷 경로/ROI가 있다(있는 폴더만).
  `templates/village_common/README.md`에 마을 공용 도장의 실측 표가 있다.
- `logs/` - 실행 로그(`YYYY-MM-DD-HHMM-NNN_start|rebootN.txt`) + 정체/재시작 증거 스샷. 4일 지나면 자동 삭제.
  **버그 조사는 여기서 시작**(아래 "도장/버그 검증 시 활용할 원본 자료" 참고).
- `dev/` (git 미추적) - `ROADMAP.md`(**추후 구현 예정 목록 - 새 세션은 이것부터 읽을 것**), `ROI_check/`
  (실측용 원본 스크린샷 모음), `template_cropper/`(도장 크롭 GUI 도구), `tools/make_release_zip.py`,
  `AutoSkillCast/`(폐기 예정 프로토타입), `fog_check/`.
- `remote_control/` - 폰(테일스케일)에서 매크로 시작/정지/상태 확인하는 로컬 HTTP 서버(`server.py`, 표준
  라이브러리만 사용). `/stop`은 python.exe와 루트 cmd.exe(`macro_cmd.pid`)를 같이 종료한다.
- `releases/` - 릴리즈 zip 백업(아래 규칙상 절대 수정/삭제 금지).
- `history.log` - 버전별 상세 수정 이력(원인/완치/검증/적용파일). **"왜 이렇게 짰지?"는 여기와 코드 주석에
  거의 다 있다** - 코드 주석이 `🚨 [날짜 실전 확인]` 형식으로 사고 경위를 길게 남기는 게 이 저장소의 관례.

### 자주 쓰는 명령
```bash
python -m py_compile src/main.py src/dungeon_bot.py   # 수정 후 항상
# 라이브 화면 확인(Git Bash, 샌드박스 해제 필요). ⚠️ 매크로/사용자가 조작 중일 수 있으니 먼저 물어볼 것
adb -s 127.0.0.1:16448 shell "screencap -p /sdcard/chk.png"
MSYS_NO_PATHCONV=1 adb -s 127.0.0.1:16448 pull /sdcard/chk.png "dev/ROI_check/chk.png"
PYTHONIOENCODING=utf-8 python dev/tools/make_release_zip.py   # 릴리즈 zip(사용자 지시 시에만)
```

### 작업 규칙 (옛 `.agents/AGENTS.md`·`새대화 이어나가기.txt`에서 이관, 아직 유효한 것만)
- **`releases/`는 백업 전용** - 어떤 파일도 수정/삭제/덮어쓰기 금지. 릴리즈는 사용자가 지시할 때만 만든다.
- **커밋은 로컬에서 자유롭게, `git push`/GitHub 릴리즈는 사용자가 명시적으로 지시할 때만.**
- 코드를 고치면 `history.log` 최상단에 상세 이력(무엇을/왜/어떻게/검증)을, 각 `src/` 파일 상단 버전
  히스토리 주석에 한두 줄 요약을 남긴다(릴리즈 절차는 아래 "일반 원칙" 참고).
- **던전 내 상자 최우선 철칙**: 던전 안에서 무슨 작업 중이든 "열다"(`t_yeolda`)가 보이면 최우선으로
  `chest_opener.open_and_disarm_chest`를 태운다.
- **검증된 시퀀스 재활용 철칙**: 기존 던전에서 검증된 순환/상태전이/힐링/숙박/이동 모듈을 최우선 재사용하고,
  임시 분기나 억지 예외(`or 특정던전`)를 새로 만들지 않는다.
- **여러 요청이 한 메시지에 오면** 항목을 먼저 목록으로 정리하고 하나도 빠짐없이 답/조치한다(사용자가
  여러 번 지적한 누락 경향).
- **새 도장을 추가할 때** 사용자에게 "배경이 반투명이라 상황에 따라 달라지나요?"를 먼저 묻고, 반투명이면
  이진화, 불투명이면 그레이스케일을 근거와 함께 추천(단 마을 공용 도장은 실측상 그레이스케일이 나았던
  예외 - `village_common/README.md`). ROI를 쓰면 도장 크기 대비 ROI 여백이 충분한지 수치로 확인해 보고.
- 사용자의 설명 없는 파일명 약어: "도장"=템플릿 이미지, "앵커"=화면 판별용 도장, "하켄"=던전 내 귀환
  포탈, "백아"=백아의 동굴, "유령성"=북쪽의 유령선(FFXI 콜라보), "대설지대"=이스벨크 마을외곽 경유 던전.

### 새 코드를 짤 때의 교훈 (대설지대 구현 재검토에서 나온 버그 8건의 공통 패턴)
1. **비슷한 두 화면을 도장으로 구분하려 들지 말 것** - 같은 단어/모양 도장은 서로 교차 매칭된다(실측
   0.895). 같은 동작을 원하면 "동작 반복 + 다른 화면의 명확한 앵커로 탈출"하는 수렴 루프로 짤 것.
2. **"이 앵커는 이 화면에만 있겠지" 금지** - 새 앵커는 다른 화면 스크린샷 전부에 돌려 오탐 점수를 재고,
   오탐이 있으면 배제 조건을 같이 넣을 것("아무것도 안 한다"가 하켄/상자/캠핑 화면에 전부 있었음).
3. **UI 상대 위치가 항상 같다고 가정 금지** - 화면에 하나뿐인 UI는 추정 말고 전체검색(가장자리에서 가로
   121px까지 밀린 사례).
4. **항상 보이는 버튼을 상태 신호로 쓰지 말 것** - 재개 버튼은 필드에 항상 있다. 상태는 변화나 배타적
   앵커로 판정.
5. numpy 배열을 `==`로 센티넬 비교하지 말 것(`ValueError`가 상위 `except`에 삼켜져 기능이 통째로 죽음).

## 버그/개선 사항 발견 시 작업 순서 - 정리 먼저, 코딩은 사용자 지시 후

### 규칙
대화 중 버그를 진단하거나 개선 사항을 발견하면, 원인/증상/수정 방향을 정리해서 알려주는 데서 일단
멈춘다. 사용자가 "코딩해"/"진행해"처럼 실제 코드 수정을 명시적으로 지시하기 전까지는 코드를 고치지
않는다. 이 저장소가 "위급하다"고 판단할 만한 사안이라도 마찬가지다 - 사용자가 별도로 "이건 지금 바로
고쳐줘"라고 하지 않는 한 정리만 해두고 사용자의 다음 지시를 기다린다. 정리해 둘 곳(로드맵/채팅 답변)은
사안 성격에 따라 판단하되, 당장 착수하지 않기로 확정된 사안은 기존 관례대로 `dev/ROADMAP.md`에 남긴다
(아래 "추후 구현 예정 사항 관리" 참고). (2026-09-14 확정)

## 화면 인식(템플릿 매칭) - 배경이 바뀔 수 있는 "선택지형" 화면은 멀티패스를 기본으로

### 배경
이 매크로는 OpenCV `cv2.matchTemplate`으로 화면 속 텍스트/버튼을 인식한다. 대부분의 도장은 로드 시
고정 이진화 문턱(보통 160)으로 한 번 이진화해두고, 실시간 화면도 같은 고정 문턱으로 이진화해서 비교한다.

문제는 화면 배경이 두 가지 방식으로 바뀔 수 있다는 것 - (a) 상태 이상 등에 따른 일시적 반투명 연출
(붉은 피안개 등), (b) 스토리 진행에 따른 배경 그래픽 자체의 교체(엔딩/구간 돌입 등). 두 경우 다
텍스트의 실제 밝기가 미묘하게 바뀌면서, 딱 하나의 고정 이진화 문턱으로는 매칭 점수가 뚝 떨어져 아예
인식이 안 되는 실전 사고가 있었다:

- **피안개(2026-08-16)**: 주인공이 빈사 상태가 되면 화면 전체에 붉은 안개가 씌워지면서 흰 글씨
  ("열다" 등)의 밝기가 내려앉아, 문턱 160에서 텍스트가 사실상 지워짐.
- **유령성 진엔딩 배경 교체(2026-08-26)**: 유령성 3회차 엔딩(진엔딩) 진입 이후 던전선택 화면의
  배경 그래픽 자체가 바뀌면서, 문턱 160 매칭 점수가 0.72로 떨어짐(임계값 0.88 미달) - 밤새 7시간
  넘게 단 한 번도 던전에 못 들어감.

두 사고 모두 원인은 같은 패턴: **매칭 신뢰도 임계값은 정상인데, 이진화 전처리 문턱 하나가 지금 이
화면 조건에 안 맞았을 뿐**이었다. 배경이 바뀌는 계기가 일시적 연출이든 스토리 진행에 따른 영구적
그래픽 교체든, 대응 원칙은 동일하다.

### 규칙
1. **"선택지형" 화면(던전선택, 메뉴, 향후 추가될 던전 내 대화 선택지 등)에서 특정 버튼/텍스트를
   찾아 클릭하는 코드는 기본적으로 멀티패스 이진화를 사용한다** - 여러 이진화 문턱을 순차 시도하고,
   하나라도 매칭 신뢰도 임계값을 넘으면 그 자리를 클릭한다. **신뢰도 임계값 자체는 절대 낮추지 말고,
   오직 이진화 전처리 문턱만 여러 개 시도할 것** (이 원칙을 어기면 임계값을 낮췄을 때처럼 다른 버튼과
   오탐하는 문제가 재발한다).
2. **전투 중 매 프레임(루프 틱마다) 반복 호출되는 상시 감시용 앵커(배속/자동 전투 판정, HP 모니터링
   등)는 굳이 멀티패스로 바꾸지 않는다** - `matchTemplate` 호출이 매번 배로 늘어나는 비용이 이득보다
   크다. 화면 전환당 한 번 정도만 클릭 판정하는 선택지/버튼류에만 기본 적용한다.
3. **새로운 이진화 문턱 조합을 추가할 때는 반드시 실제 문제가 재현된 실제 스크린샷으로 검증**하고
   추가한다 - 감으로 문턱 값을 정하지 말 것. 열화 원인에 따라 필요한 방향이 다르다(피안개처럼 화면이
   어두워지는 경우엔 문턱을 낮춰야 하고, 유령성 진엔딩 배경처럼 미묘하게 밝기가 흔들리는 경우엔 오히려
   160보다 살짝 낮은 값 하나만 필요했다 - 100/85 같은 값은 오히려 점수를 더 떨어뜨렸다). 기존에
   검증된 문턱 세트를 다른 상황에 그대로 재사용하지 말고, 매번 해당 화면으로 직접 측정할 것.

### 기존 구현 참고
- `src/dungeon_bot.py`의 `check_template_present_multipass()` / `check_template_present_dynamic()`
  - 상자/피안개 관련 앵커에 사용, `FOG_BIN_PASSES = (160, 100, 85)`.
- `src/main.py`의 `find_and_click_template_multipass()`
  - 유령성 던전선택 '2nd' 층 버튼(3회차 진엔딩 이후 배경)에 사용, `bin_passes=(160, 150)`.

새 선택지형 화면을 추가할 때는 위 두 함수 중 하나를 재사용하거나 같은 패턴으로 새 헬퍼를 만들 것.

## 도장 인식 방식을 고칠 때 - 같은 도장을 쓰는 다른 스크립트도 반드시 전수 점검

### 배경
`src/main.py`, `src/dungeon_bot.py`, `src/chest_opener.py`, `src/inn_manager.py`, `src/party_manager.py`는
서로 `import`해서 공유하는 게 아니라 **`load_template`/`load_grayscale_template`/`load_color_template`
같은 로더 함수를 파일마다 각자 복사해서 갖고 있다.** 같은 도장 파일(`templates/....png`)을 여러 파일이
동시에 참조하는 경우도 흔한데, 이때 파일마다 "이진화(160) 후 매칭"과 "원본 그레이스케일 매칭"처럼 인식
방식이 서로 다르면, 한쪽에서만 문제를 고치고 다른 쪽은 옛날 방식 그대로 남아 있어도 아무 에러 없이
조용히 돌아간다 - 겉으로는 멀쩡해 보이다가 특정 화면 상태에서만 한쪽만 실패하는 형태로 터진다.

- **전투 인식 실패(2026-08-27)**: `templates/combat_in.png`/`combat_slow.png`(배속 버튼 앵커)를
  `dungeon_bot.py`는 이미 hotfix8에서 "원본 그레이스케일 매칭"으로 고쳐놨는데, `main.py`의 부팅 복구
  재분류 로직은 예전 그대로 "이진화(160) 매칭"을 쓰고 있었음. 평소엔 둘 다 통과해서 티가 안 났지만,
  스킬 메뉴가 펼쳐진 수동 턴 대기 화면에서만 이진화가 미세 명암을 뭉개 점수가 0.3~0.55(임계값 0.80
  미달)로 떨어짐 - `dungeon_bot.py`로 넘어갔으면 그레이스케일 매칭으로 0.86~0.92가 나와 정상 인식됐을
  화면인데, `main.py`가 먼저 "아웃게임 무반응 정체"로 오판해 5분마다 헛도는 전체 재부팅을 반복함.
  같은 감사(전수조사)에서 `templates/close_panel.png`/`confirm_recover.png`/`healer_auto_btn.png`도
  `dungeon_bot.py`(이진화)와 `party_manager.py`(그레이스케일, 모듈 레벨 `G_` 캐시) 사이에 같은 방식
  불일치가 있었음 - 이쪽은 `dungeon_bot.py`가 넘기는 이진화 버전 파라미터(`t_heal_auto`/`t_heal_close`)를
  `party_manager.run_party_healing_sequence()`가 실제로는 안 쓰고 자체 그레이스케일 전역변수만 참조하는
  구조라 당장 오작동으로 이어지진 않았지만(죽은 파라미터), 리팩터링 잔재가 방치된 동일 패턴이었음.

- **동결 상태이상 전투 미인식(2026-09-16)**: 동결(freeze) 상태이상의 시각효과가 `combat_in.png`/
  `combat_slow.png` 매칭 점수를 0.75 안팎으로 떨어뜨려, 신뢰도 임계값 0.80을 쓰는 자리마다 전투를
  못 알아보는 사고. 처음엔 "부팅 복구 분류기(사령탑) 1곳"만 고치면 될 줄 알았는데, 같은 도장을 쓰는
  호출부를 `t_combat_in`/`t_combat_slow` **변수명 기준**으로 전수 grep해보니 `main.py`에 1곳,
  **`dungeon_bot.py` 한 파일 안에만 0.80 하드코딩이 7곳**(전투 진입 1차/2차 게이트, TRIGGER_EXIT
  기습감지, 필드맵 귀환 루틴 3곳, 하켄 귀환 대기 1곳) 더 있었음 - 기존 규칙 1의 예시 명령
  (`grep -rn "combat_in.png" src/*.py`)은 도장이 **로드되는** 지점만 찾아주지, 그 변수를 쓰는 개별
  호출부의 각자 다른 임계값까지는 안 잡아준다는 게 뒤늦게 드러남. "한 파일 안에서도 같은 앵커를 쓰는
  호출부가 여러 개면 전부 같은 값을 쓰고 있는지" 확인이 빠져 있었던 것.

### 규칙
1. **어떤 도장의 인식 방식(이진화 문턱, 그레이스케일/컬러 매칭, 크롭 영역, 신뢰도 임계값)을 고치면,
   그 도장 파일 경로(`templates/....png`)를 참조하는 다른 모든 `.py` 파일을 grep으로 찾아 같은 방식을
   쓰고 있는지 반드시 확인한다.** 아래 명령으로 한 도장을 쓰는 모든 위치를 한 번에 찾을 수 있다:
   ```bash
   grep -rn "combat_in.png" src/*.py   # 파일명만 바꿔서 재사용
   ```
2. **한 곳(한 호출부)의 임계값을 고쳤다면, 반드시 그 도장을 로드해 쓰는 변수명(예: `t_combat_in`)으로
   전수 grep해서 "같은 앵커를 쓰는 다른 모든 호출부"의 임계값도 같이 확인한다 - 같은 파일 안이라도
   예외 없다.** 위 2026-09-16 사고처럼 한 파일 안에 같은 앵커를 쓰는 호출부가 여러 개 흩어져 있을 수
   있고, grep 결과로 나온 각 호출부의 임계값이 서로 다르면(예: 한쪽은 0.70, 다른 쪽은 0.80) 의도된
   차이(예: "상시 감시용이라 낮게" vs "오탐 방지용으로 일부러 높게")인지 방치된 불일치인지 반드시
   구분한다. 명확한 이유가 없다면 이미 실전 검증된 값(보통 더 자주 실행되며 오탐 없이 오래 쓰인 쪽)
   으로 통일한다.
3. 여러 파일이 같은 도장을 참조하면서 로더/매칭 함수가 다르면(예: `load_template` vs
   `load_grayscale_template`, 또는 매칭 시 이진화를 거치는지 여부), **왜 다른지 먼저 확인한다** -
   의도된 차이(예: 서로 다른 크롭 영역이나 용도)인지, 한쪽만 고쳐지고 다른 쪽은 방치된 잔재인지 구분
   해야 한다. 후자면 방금 검증된(실전 스크린샷으로 통과 확인된) 방식으로 나머지도 맞춘다.
4. 함수에 도장 템플릿을 파라미터로 넘기는 구조라면, **그 파라미터가 함수 내부에서 실제로 쓰이는지도
   같이 확인한다** - 리팩터링 중 내부 구현만 바뀌고 호출부의 파라미터 전달은 안 지워진 죽은 코드가
   섞여 있을 수 있다(위 `t_heal_auto`/`t_heal_close` 사례).
5. 수정 후에는 실제 문제가 재현된 스크린샷(또는 최소한 실제 로그로 확보한 스코어)으로 새 방식이 통과
   하는지 재확인한다 - 감으로 "그레이스케일이 더 낫겠지"라고 바꾸지 말 것(§일반 원칙의 추측 금지 원칙과
   동일).

## 화면 진행 감지 시 정체 카운터(`transition_delay_count`) 리셋 - 공용 지점과 반드시 대조

### 배경
`src/dungeon_bot.py`의 `start_main_macro()`는 수십 개의 실전 사고 대응 핫픽스로 계속 몸집을 키워온
거대한 단일 함수다. "화면이 진짜로 진행됐다"(필드 안착, 전투 조우, 사망 감지 등)는 판정이 나오면 대부분
공용 전처리 블록(`~1440-1560` 부근)에서 `transition_delay_count = 0`으로 리셋하는데, 이 블록 앞이나
안쪽에 끼어있는 특수 케이스 분기들은 새로 추가될 때마다 "그 자리에서 감지 코드를 다시 짜 넣는" 식으로
늘어나서, 공용 리셋 로직을 매번 재사용하지 않고 카운터 리셋을 깜빡 빠뜨리는 사고가 반복됐다:

- **유령성4층 3층→4층 진입(2026-09-05)**: `dungeon_bot.py:1269-1293`이 3층 필드 앵커를 감지하고도
  `transition_delay_count`를 리셋하지 않아, 서로 독립된 두 번의 정상 로딩(3층 진입 대기 + 4층 진입 대기)
  이 하나의 10회 예산으로 합쳐져 카운트됨(사용자 실기 확인: "1 2 3 4 카운팅하고... 다시 5 6 7 8로
  카운팅").
- **정체 30초 복구 중 사망 감지(2026-09-05)**: 위 사고를 계기로 전수 조사한 결과, `dungeon_bot.py:1389`
  의 정체(stuck) 복구 메가블록 안에서도 "공식" 사망 감지 분기(`~1413-1426`)와 완전히 동일한
  `get_dead_match_score(...) > 0.65` 체크를 또 하면서 `transition_delay_count` 리셋만 빠뜨린 쌍둥이
  분기가 발견됨 - 같은 유형의 버그가 하루 만에 두 번째로 확인됨.

두 사고 모두 `continue`로 메인 루프에 돌아가는 분기라 다음 틱까지 카운터가 그대로 이어지는 위험이 있었다.
전면 재작성(헬퍼 함수화 등)은 이 프로젝트 규모 대비 과한 리스크라 판단해 하지 않기로 했고, 대신 이
컨벤션으로 재발을 방지한다.

### 규칙
1. **화면 진행(필드 안착, 전투 조우, 사망/부활 감지, 하켄 메뉴 처리 등)을 새로 감지하는 특수 케이스
   분기를 추가할 때는, 먼저 공용 전처리 블록(`~1440-1560` 부근)에 이미 동일하거나 비슷한 감지 로직이
   있는지 확인하고, 있다면 그 옆의 `transition_delay_count = 0` 리셋도 반드시 같이 넣는다.**
2. 기존 코드에 이미 비슷한 감지 로직이 두 곳 이상 있다면(예: `get_dead_match_score` 체크가 여러 분기에
   중복 존재), 한쪽만 카운터를 리셋하고 있지 않은지 diff해서 확인한다 - 의도된 차이가 아니라면 방치된
   누락일 가능성이 높다.
3. 새 분기가 `continue`로 메인 루프에 복귀하는 게 아니라 `return`/`raise`로 함수를 완전히 빠져나간다면
   이 리셋은 필요 없다(다음 틱까지 카운터가 이어질 일 자체가 없음).

## 새 던전을 코드에 통합할 때 - 기존 던전의 "복귀 후 처리" 부수효과를 전부 대조

### 배경
바로 위 "정체 카운터 리셋" 항목과 본질적으로 같은 유형의 결함이 또 다른 변수에서 재발했다:

- **대설지대 주회 한도 도달 시 무한 왕복(2026-09-10)**: `LIMIT_DUNGEON_LOOPS`(마을 회군 기준)에
  도달하면 여관을 들러 회군하는 로직을, 백아/유령성은 이미 갖고 있었다(`dungeon_run_count = 0` /
  `is_fully_healed = True` / `need_pickaxe_refill = False`를 여관 취침 직후 함께 리셋). 대설지대를
  추가할 때 여관 경유 자체(`resupply_mode="inn"`)는 새로 짰지만, **`dungeon_run_count = 0` 리셋을
  빠뜨렸다** - 컴파일도 통과하고 실행도 되지만, 주회 한도에 도달한 뒤 여관을 다녀와도 재진입 거부
  상태가 절대 안 풀려서 "마을외곽 ↔ 던전 경로목록" 무한 왕복에 빠졌다(실전 로그: 약 2시간 방치).
  뮤뮤나 ADB 문제가 전혀 아닌, 순수하게 "기존 던전이 하던 부수효과 중 하나를 빠뜨린" 로직 결함이었다.

두 사고의 공통점: 새 코드 경로가 **기존 패턴의 "주된 동작"만 복제하고, 그 지점에서 같이 일어나야 하는
부수효과(카운터/플래그 리셋)는 빠뜨려도 아무 에러 없이 조용히 넘어간다.** 그러다 그 부수효과가 실제로
필요해지는 조건(정체 30초, 주회 한도 도달 등)에 도달했을 때만 뒤늦게 무한루프/정체로 터진다 - 그래서
개발 중에는 안 걸리고 장시간 무인 방치 중에만 걸리는 경우가 많다.

### 규칙
1. **새 던전(또는 새 지역/새 귀환 경로)을 추가하면서 "기존 던전도 이미 하고 있는 동작"(마을/여관
   복귀, 주회 한도 처리, 정체 복구 등)을 새로 짤 때는, 반드시 기존 던전의 같은 지점 코드를 먼저 찾아
   나란히 놓고 비교한다.** 예: `grep -n "dungeon_run_count = 0" src/main.py`처럼 그 변수/플래그가
   리셋되는 모든 지점을 찾고, 새로 짜는 지점도 같은 리스트에 포함되는지 확인.
2. 특히 다음 두 종류는 "주된 동작(화면 인식/클릭)"과 분리해서 별도로 체크리스트화할 것 - 둘 다 없어도
   당장 에러가 안 나서 놓치기 쉽다:
   - **되돌아가야 하는 카운터/플래그** (`dungeon_run_count`, `transition_delay_count`,
     `heavysnow_resupply_attempts` 등 "다음 사이클이 정상 진행되려면 0으로 돌아가 있어야 하는" 값)
   - **다음 단계로 이어지는 트리거 플래그** (`is_fully_healed`, `need_pickaxe_refill`,
     `heavysnow_force_inn_this_cycle` 등 "이 지점 이후의 판단 로직이 참조하는" 값)
3. 대조 결과 기존 던전과 의도적으로 다르게 짠 부분이 있다면(예: 이 던전만 캠핑으로 회복해서 여관이
   필수가 아님) 왜 다른지 주석으로 남긴다 - 그래야 다음에 또 다른 사람/세션이 "빠뜨린 건지 의도한
   건지"를 코드만 보고 구분할 수 있다.
4. 가능하면 실제로 그 조건(정체 임계값, 주회 한도 등)에 도달하는 시나리오까지 실기로 재현해 검증한다
   - 평소 짧은 테스트 주행으로는 이런 종류의 결함이 절대 안 드러난다(§일반 원칙의 추측 금지 원칙과
   같은 이유 - 조건이 실제로 걸리는 상황을 직접 만들어봐야 확인된다).
5. **새 던전 추가 요청을 받으면 사용자에게 이 정보를 받는다**(옛 `dungeon_template_guide.md` 양식 대체):
   던전명 / 진입 방식(세계지도 직행형 vs 마을 경유형) / 출발·복귀 마을 / 필요한 도장(세계지도 아이콘,
   던전선택 화면 앵커, 마을외곽 버튼·경로 행, 층 선택 버튼 - 반투명 여부 포함) / 층 목록과 층별 주회방식
   (상자파밍·광석파밍) / 귀환 방식(나가기·하켄·캠핑, `presets.json` `_가이드`의 `return_method` 값) /
   특수 파밍 도장. 코드 쪽 등록 지점은 `presets.json` 프리셋 + `main.py` `PRESET_ID_MAP` + 루트 `.bat`
   (기존 `.bat`을 복사해 영문ID·title만 교체 - 슈퍼바이저 루프 유지), 그리고 세계지도 아이콘 분기(ROADMAP
   10번 - 세 번째 마을/직행형 던전을 추가할 땐 딕셔너리 리팩터링부터 할 것).

## 도장/버그 검증 시 활용할 원본 자료 - 채팅 첨부 이미지에 의존하지 말 것

### 배경
채팅에 붙여넣은 스크린샷은 파일로 직접 읽을 수 없어 실측 검증이 막힌다. 그런데 이 저장소에는 이미
그 검증에 쓸 수 있는 원본 자료가 두 군데 남아 있다:

- **2026-09-12 사망 감지 도장(`templates/Field/stat_dead.png`) 검증**: 채팅 첨부 스크린샷을 파일로
  확보 못 해 검증이 막혔었는데, 크롭 도구가 도장을 만들 때마다 같은 폴더에 남기는
  `_crop_metadata.json`에 원본 스크린샷 경로(`source_image`)가 이미 저장돼 있었다 - 이 도장의 경우
  `dev/ROI_check/사망.png`. 이걸 먼저 확인했으면 채팅 이미지 없이도 바로 실제 파일로 검증할 수
  있었다.

### 규칙
1. **기존 도장 파일의 원본 출처(어떤 실제 화면에서 언제 크롭됐는지)가 필요하면, 같은 폴더의
   `_crop_metadata.json`을 먼저 확인한다** - 그 도장 키의 `source_image` 필드가 원본 스크린샷
   파일명이다. 보통 `dev/ROI_check/` 밑에 있으니 `Glob`으로 파일명을 검색해 찾는다. 이렇게 찾은
   원본은 크롭된 조각(예: 아이콘만)보다 정보가 많아 - 밝기가 정상적인지, 화면의 다른 요소와 오탐
   나는지, 실제 위치가 어디인지 - 검증에 훨씬 유리하다.
2. **사용자가 채팅에 에러/버그 로그를 붙여넣을 때, 그 로그 안에 "📸 [스크린샷 동기화] 새 스크린샷이
   감지되어 로그 폴더로 카피되었습니다: `<파일명>`" 같은 줄이 있으면, 그 파일이 `logs/` 폴더에 실제로
   존재한다.** 사용자가 상황을 재현/설명하려고 그 시점에 직접 캡처해둔 경우가 많으므로, 채팅에 이미지가
   같이 첨부됐더라도 파일로 못 읽는다고 포기하지 말고 로그에 이런 줄이 있는지부터 확인한다 - 있으면
   그 파일이 바로 실측 검증에 쓸 수 있는 실제 자료다.
3. 위 두 경로 모두에서 원본을 못 찾았을 때만 채팅 첨부 이미지 설명에 의존해 추측하고, 그 경우 반드시
   "실측하지 못했다"는 점을 사용자에게 명시한다(§일반 원칙의 추측 금지 원칙과 동일).

## 추후 구현 예정 사항 관리
대화 중 "나중에 하자"고 합의만 하고 바로 구현하지 않는 기능/개선 사항은 전부 `dev/ROADMAP.md`에 정리한다.
사용자가 추후 구현 목록에 대해 물어보면 이 파일 내용을 기준으로 답한다. 새로 합의되는 항목은 이 파일에
추가하고, 실제로 구현이 완료되면 **해당 항목을 이 파일에서 지운다**(구현 이력은 `history.log`와 커밋
메시지에 남기므로 중복 보관하지 않음 - 2026-09-24 정리 때 확정). 일부만 구현됐으면 남은 부분만 남긴다.
폐기 결정된 아이디어는 재검토 방지용으로 "❌ 검토 후 폐기" 섹션에 이유와 함께 남긴다. 이 파일은 계속
업데이트되는 살아있는 문서다. **"나중에 할 일" 메모를 루트나 `dev/`에 별도 파일로 만들지 말 것** - 전부
이 파일 하나에 모은다(예전에 `1.16.0 예정.txt`, `implementation_plan.md`, `dev/대설지대_구현지침.md` 등이
흩어져 쌓였다가 2026-09-24에 전부 이 파일로 통합 후 삭제함).

## 일반 원칙
- 모든 임계값/색상/좌표 수정은 실제 스크린샷 또는 실제 로그로 검증 후 반영한다(추측 금지).
- 버전 릴리즈 시 `CURRENT_VERSION`(main.py) + `main.py`/`dungeon_bot.py`/`remote_control/server.py`의
  버전 히스토리 주석 + `README.md` 배너 + `history.log`를 함께 갱신한다.
  (예전 절차에 있던 `Daphne Antigravity.bat` 타이틀 갱신은 삭제 - 지금 `.bat`은 프리셋별로 분리되면서
  타이틀에 버전이 안 들어간다.)
- 릴리즈 압축은 손으로 만들지 말고 `python dev/tools/make_release_zip.py`를 쓴다. **깃이 추적하는 파일
  전부**를 담고 AI 지침 파일(`CLAUDE.md`/`AGENTS.md`)과 wvd 자산(`templates/SkillCast/`, `*_wvd.*`)만
  빼는 방식이라, 새 폴더/파일을 추가해도 자동으로 포함된다(과거 수동 압축 때 `readme_img/`를 빠뜨릴 뻔한
  사고가 있었음). README에 이미지를 넣을 때는 루트가 아니라 `readme_img/`에 두고 상대경로로 참조할 것.
