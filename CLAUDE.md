# 코딩 컨벤션 (AI 어시스턴트용)

이 저장소에서 작업하는 모든 AI 코딩 어시스턴트(Claude 외 다른 도구 포함)가 따라야 할 규칙입니다.
동일 내용이 `AGENTS.md`에도 있습니다.

## 화면 인식(템플릿 매칭) - 배경이 바뀔 수 있는 "선택지형" 화면은 멀티패스를 기본으로

### 배경
이 매크로는 OpenCV `cv2.matchTemplate`으로 화면 속 텍스트/버튼을 인식한다. 대부분의 도장은 로드 시
고정 이진화 문턱(보통 160)으로 한 번 이진화해두고, 실시간 화면도 같은 고정 문턱으로 이진화해서 비교한다.

문제는 화면 배경이 두 가지 방식으로 바뀔 수 있다는 것 - (a) 상태 이상 등에 따른 일시적 반투명 연출
(붉은 피안개 등), (b) 스토리 진행에 따른 배경 그래픽 자체의 교체(엔딩/구간 돌입 등). 두 경우 다
텍스트의 실제 밝기가 미묘하게 바뀌면서, 딱 하나의 고정 이진화 문턱으로는 매칭 점수가 뚝 떨어져 아예
인식이 안 되는 실전 사고가 있었다:

- **피안개(2026-08-16)**: 주인공이 빈사 상태가 되면 화면 전체에 붉은 안개가 씌워지면서 흰 글씨
  ("열다" 등)의 밝기가 내려앉아, 문턱 160에서 텍스트가 사실상 지워짐.
- **유령성 진엔딩 배경 교체(2026-08-26)**: 유령성 3회차 엔딩(진엔딩) 진입 이후 던전선택 화면의
  배경 그래픽 자체가 바뀌면서, 문턱 160 매칭 점수가 0.72로 떨어짐(임계값 0.88 미달) - 밤새 7시간
  넘게 단 한 번도 던전에 못 들어감.

두 사고 모두 원인은 같은 패턴: **매칭 신뢰도 임계값은 정상인데, 이진화 전처리 문턱 하나가 지금 이
화면 조건에 안 맞았을 뿐**이었다. 배경이 바뀌는 계기가 일시적 연출이든 스토리 진행에 따른 영구적
그래픽 교체든, 대응 원칙은 동일하다.

### 규칙
1. **"선택지형" 화면(던전선택, 메뉴, 향후 추가될 던전 내 대화 선택지 등)에서 특정 버튼/텍스트를
   찾아 클릭하는 코드는 기본적으로 멀티패스 이진화를 사용한다** - 여러 이진화 문턱을 순차 시도하고,
   하나라도 매칭 신뢰도 임계값을 넘으면 그 자리를 클릭한다. **신뢰도 임계값 자체는 절대 낮추지 말고,
   오직 이진화 전처리 문턱만 여러 개 시도할 것** (이 원칙을 어기면 임계값을 낮췄을 때처럼 다른 버튼과
   오탐하는 문제가 재발한다).
2. **전투 중 매 프레임(루프 틱마다) 반복 호출되는 상시 감시용 앵커(배속/자동 전투 판정, HP 모니터링
   등)는 굳이 멀티패스로 바꾸지 않는다** - `matchTemplate` 호출이 매번 배로 늘어나는 비용이 이득보다
   크다. 화면 전환당 한 번 정도만 클릭 판정하는 선택지/버튼류에만 기본 적용한다.
3. **새로운 이진화 문턱 조합을 추가할 때는 반드시 실제 문제가 재현된 실제 스크린샷으로 검증**하고
   추가한다 - 감으로 문턱 값을 정하지 말 것. 열화 원인에 따라 필요한 방향이 다르다(피안개처럼 화면이
   어두워지는 경우엔 문턱을 낮춰야 하고, 유령성 진엔딩 배경처럼 미묘하게 밝기가 흔들리는 경우엔 오히려
   160보다 살짝 낮은 값 하나만 필요했다 - 100/85 같은 값은 오히려 점수를 더 떨어뜨렸다). 기존에
   검증된 문턱 세트를 다른 상황에 그대로 재사용하지 말고, 매번 해당 화면으로 직접 측정할 것.

### 기존 구현 참고
- `src/dungeon_bot.py`의 `check_template_present_multipass()` / `check_template_present_dynamic()`
  - 상자/피안개 관련 앵커에 사용, `FOG_BIN_PASSES = (160, 100, 85)`.
- `src/main.py`의 `find_and_click_template_multipass()`
  - 유령성 던전선택 '2nd' 층 버튼(3회차 진엔딩 이후 배경)에 사용, `bin_passes=(160, 150)`.

새 선택지형 화면을 추가할 때는 위 두 함수 중 하나를 재사용하거나 같은 패턴으로 새 헬퍼를 만들 것.

## 도장 인식 방식을 고칠 때 - 같은 도장을 쓰는 다른 스크립트도 반드시 전수 점검

### 배경
`src/main.py`, `src/dungeon_bot.py`, `src/chest_opener.py`, `src/inn_manager.py`, `src/party_manager.py`는
서로 `import`해서 공유하는 게 아니라 **`load_template`/`load_grayscale_template`/`load_color_template`
같은 로더 함수를 파일마다 각자 복사해서 갖고 있다.** 같은 도장 파일(`templates/....png`)을 여러 파일이
동시에 참조하는 경우도 흔한데, 이때 파일마다 "이진화(160) 후 매칭"과 "원본 그레이스케일 매칭"처럼 인식
방식이 서로 다르면, 한쪽에서만 문제를 고치고 다른 쪽은 옛날 방식 그대로 남아 있어도 아무 에러 없이
조용히 돌아간다 - 겉으로는 멀쩡해 보이다가 특정 화면 상태에서만 한쪽만 실패하는 형태로 터진다.

- **전투 인식 실패(2026-08-27)**: `templates/combat_in.png`/`combat_slow.png`(배속 버튼 앵커)를
  `dungeon_bot.py`는 이미 hotfix8에서 "원본 그레이스케일 매칭"으로 고쳐놨는데, `main.py`의 부팅 복구
  재분류 로직은 예전 그대로 "이진화(160) 매칭"을 쓰고 있었음. 평소엔 둘 다 통과해서 티가 안 났지만,
  스킬 메뉴가 펼쳐진 수동 턴 대기 화면에서만 이진화가 미세 명암을 뭉개 점수가 0.3~0.55(임계값 0.80
  미달)로 떨어짐 - `dungeon_bot.py`로 넘어갔으면 그레이스케일 매칭으로 0.86~0.92가 나와 정상 인식됐을
  화면인데, `main.py`가 먼저 "아웃게임 무반응 정체"로 오판해 5분마다 헛도는 전체 재부팅을 반복함.
  같은 감사(전수조사)에서 `templates/close_panel.png`/`confirm_recover.png`/`healer_auto_btn.png`도
  `dungeon_bot.py`(이진화)와 `party_manager.py`(그레이스케일, 모듈 레벨 `G_` 캐시) 사이에 같은 방식
  불일치가 있었음 - 이쪽은 `dungeon_bot.py`가 넘기는 이진화 버전 파라미터(`t_heal_auto`/`t_heal_close`)를
  `party_manager.run_party_healing_sequence()`가 실제로는 안 쓰고 자체 그레이스케일 전역변수만 참조하는
  구조라 당장 오작동으로 이어지진 않았지만(죽은 파라미터), 리팩터링 잔재가 방치된 동일 패턴이었음.

### 규칙
1. **어떤 도장의 인식 방식(이진화 문턱, 그레이스케일/컬러 매칭, 크롭 영역, 신뢰도 임계값)을 고치면,
   그 도장 파일 경로(`templates/....png`)를 참조하는 다른 모든 `.py` 파일을 grep으로 찾아 같은 방식을
   쓰고 있는지 반드시 확인한다.** 아래 명령으로 한 도장을 쓰는 모든 위치를 한 번에 찾을 수 있다:
   ```bash
   grep -rn "combat_in.png" src/*.py   # 파일명만 바꿔서 재사용
   ```
2. 여러 파일이 같은 도장을 참조하면서 로더/매칭 함수가 다르면(예: `load_template` vs
   `load_grayscale_template`, 또는 매칭 시 이진화를 거치는지 여부), **왜 다른지 먼저 확인한다** -
   의도된 차이(예: 서로 다른 크롭 영역이나 용도)인지, 한쪽만 고쳐지고 다른 쪽은 방치된 잔재인지 구분
   해야 한다. 후자면 방금 검증된(실전 스크린샷으로 통과 확인된) 방식으로 나머지도 맞춘다.
3. 함수에 도장 템플릿을 파라미터로 넘기는 구조라면, **그 파라미터가 함수 내부에서 실제로 쓰이는지도
   같이 확인한다** - 리팩터링 중 내부 구현만 바뀌고 호출부의 파라미터 전달은 안 지워진 죽은 코드가
   섞여 있을 수 있다(위 `t_heal_auto`/`t_heal_close` 사례).
4. 수정 후에는 실제 문제가 재현된 스크린샷(또는 최소한 실제 로그로 확보한 스코어)으로 새 방식이 통과
   하는지 재확인한다 - 감으로 "그레이스케일이 더 낫겠지"라고 바꾸지 말 것(§일반 원칙의 추측 금지 원칙과
   동일).

## 추후 구현 예정 사항 관리
대화 중 "나중에 하자"고 합의만 하고 바로 구현하지 않는 기능/개선 사항은 전부 `dev/ROADMAP.md`에 정리한다.
사용자가 추후 구현 목록에 대해 물어보면 이 파일 내용을 기준으로 답한다. 새로 합의되는 항목은 이 파일에
추가하고, 실제로 구현이 완료되면 해당 항목을 `⏳ 예정`에서 `✅ 구현완료`로 옮겨 적는다(완료일과 무엇을
어떻게 고쳤는지 한두 줄로 남길 것). 이 파일은 계속 업데이트되는 살아있는 문서이지, 한 번 쓰고 마는 기록이
아니다.

## 일반 원칙
- 모든 임계값/색상/좌표 수정은 실제 스크린샷 또는 실제 로그로 검증 후 반영한다(추측 금지).
- 버전 릴리즈 시 `CURRENT_VERSION`(main.py) + `main.py`/`dungeon_bot.py`/`remote_control/server.py`의
  버전 히스토리 주석 + `Daphne Antigravity.bat` 타이틀 + `README.md` 배너 + `history.log`를 함께 갱신한다.
