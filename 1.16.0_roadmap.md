# Wizardry Daphne Kor Macro - v1.16.0 개발 로드맵

대설지대 주행 대응, 미니맵 기반 스턱 감지 개편, 자동 전투 스킬 시스템 도입 등 주요 편의성과 기믹 대응을 위한 마일스톤 관리 문서입니다. 세션이 끊겨도 에이전트와 사용자가 진행 현황을 지속적으로 연계하기 위해 관리합니다.

---

## 🎯 전체 마일스톤 및 진척률 (Progress: [ ] 0%)

- [ ] **1단계: 상자 개봉 후 팝업 범용 대화창 화살표(`dialogue_indicator.png`) 터치 개편**
  - **상세**: 
    - 미니게임 연타 후 아이템 획득창 감지 방식을 기존 `get_item.png` ➔ 우하단 공통 대화 화살표 `dialogue_indicator.png` 그레이스케일 매칭으로 전격 교체.
    - **오진 방지 ROI 가드 탑재**: 전체 화면 탐색 시 로딩 화면 등에서 대화 화살표가 오진되는 현상을 원천 방지하기 위해, 우하단 정밀 ROI `(1293, 2349, 1377, 2433)` 내부에서만 대화 화살표가 존재하는지 판단하는 전용 도우미 함수 `check_dialogue_indicator_present` 구현 및 임계값 상향(`0.65` ➔ `0.75`).
    - 화살표 직접 클릭 시 필드 오클릭 위험 차단을 위해 안전지대 고정 좌표 `(701, 333)` 터치 유지.
    - **공포 상태이상 캐릭 오터치 복구 탑재**: 공포 캐릭터 선택 시 미니게임창 대신 등장하는 "열 수 없다" 대화 팝업에서도 동일 화살표(`99.91%` 일치 확인)가 감지되므로, 안전지대 터치로 팝업을 닫고 다시 캐릭터 선택창으로 복귀하여 정상 캐릭터를 재클릭하도록 자가 복구 연동.
    - 상자 관련 도장들(`yeolda_clean.png`, `get_item.png`, `trap_minigame_anchor.png`)을 전용 폴더 `templates/chestopening/`로 이동 및 코드 연동.
  - **사령탑 크래시 방지 보완 (추가)**:
    - 화면 고정 stuck 변수(`first_stuck_time_str`)와 아웃게임 무반응 정체 변수가 하나의 변수를 오염 공유하여 `TypeError` (NoneType 연산 오류) 크래시 및 비정상 리부트를 유발하던 결함을 해결하기 위해, 아웃게임 전용 감시 변수 `first_outgame_stuck_time_str` 및 `first_outgame_stuck_start_time` 으로 격리 분리 선언.
- [ ] **2단계: 상자 파밍 후 복귀 및 토스트 메시지 인지 개편**
  - **상세**:
    - `no_chest.png` 파일을 `Field/` 폴더 하위로 이동하면서 `toastmsg_nochest.png`로 파일명 정돈.
    - 경로 탐색 실패 토스트 `Field/toastmsg_noway.png` 추가.
    - `escape_triggered` (또는 `should_exit` 등) 가칭 함수의 탈출 플래그 분기를 두어 하켄 터치 자동이동 귀환 또는 계단 귀환 시스템 다변화.
- [ ] **3단계: 대설지대(폭설) 스턱 감지 및 미니맵 노란색 커서 인식 개편**
  - **상세**:
    - 대설지대 눈보라 투영 오인식 차단을 위해 미니맵 내부의 노란색 방향 커서(`cursor (1).png` ~ `cursor (4).png`)가 정지해 있는지로 정체 여부 판단. (ROI: 1194~1245, 194~248)
    - 폭설로 지도가 가려져 커서 소실 시: 1번 이전 행동 반복 클릭 ➔ 20초 경과 시 2번 나가기 클릭 후 폭설 구간 탈출 감지.
- [ ] **4단계: 행상인/뼈상인 대화 선택창 대응 및 스크린샷 자동화**
  - **상세**:
    - 돌발 행상인/뼈상인 출현으로 대화 멈춤 시, 에뮬레이터 강제 재부팅 직전에 현재 화면을 `logs/captured_stuck/`에 저장하는 캡처 자동화 구축. (향후 선택지 도장 수집 용도)
- [ ] **5단계: 동상 기믹 대응 및 타 마을 여관 인식 개선**
  - **상세**:
    - 동상 상태이상(일반 힐 불가)에 따른 특수 메시지 인지 대응.
    - 루크나리아 마을 등 타 구역 여관 마크 이진화 ➔ 그레이스케일/컬러 매칭 전환 및 감지율 보완.
- [ ] **6단계: 캐릭터별 자동 스킬 및 BGR/컬러 템플릿 스케일링**
  - **상세**:
    - wvd의 캐릭터별 자동 스킬 전략을 분석하여 incombat 상태에서 좌상단 캐릭터 활성화 아이콘 컬러 매치 연동.
    - wvd 리소스의 1.6배 실시간 보간 스케일링(`cv2.resize`)을 통한 검출 성공률 검증.

---

## 📝 개발 이력 및 비고
- **2026-07-29**: v1.16.0 로드맵 영구 문서화 파일 개설. (1단계 상자 대화창 화살표 터치 개편 진행 합의)
